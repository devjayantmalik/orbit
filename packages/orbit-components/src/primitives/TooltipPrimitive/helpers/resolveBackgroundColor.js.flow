// @flow
import type { Interpolation } from "styled-components";

import type { Theme } from "../../../defaultTheme";

export type Props = {
  help: boolean,
  error: boolean,
  theme: Theme,
};

type ResolveBackgroundColor = Props => Interpolation;

declare export default ResolveBackgroundColor;
